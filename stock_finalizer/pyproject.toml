[project]
name = "stock_finalizer"
version = "0.1.0"
description = "stock_finalizer using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.4"
]

[project.scripts]
stock_finalizer = "stock_finalizer.main:run"
run_crew = "stock_finalizer.main:run"
train = "stock_finalizer.main:train"
replay = "stock_finalizer.main:replay"
test = "stock_finalizer.main:test"
run_with_trigger = "stock_finalizer.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
